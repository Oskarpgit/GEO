# Server-only secret. Never prefix with NEXT_PUBLIC_ and never commit a real value.
FIRECRAWL_API_KEY=

# basic = predictable lowest scrape cost; auto/enhanced may use more credits.
FIRECRAWL_PROXY_MODE=basic
